# ESTIMATES computed by API Evangelist — not published by the provider. See `note` and `replace_by`.
generated: '2026-09-20'
method: estimated
estimator: 'planning/api-economics/estimate_prices.py — design: ESTIMATION.md'
note: ESTIMATES, not quotes. Computed by API Evangelist from the prices this provider publishes on https://thingsboard.io/pricing/
  (transcribed in plans/thingsboard-plans-pricing.yml). Nothing here overrides a published price. Each
  estimate names the figure it came from, the formula, any assumption, and the field the provider can
  publish to replace it — publish that and the estimate is retired.
audience_of_source_pricing: business-product
pricing_basis: tiered-subscription
delivery: open-core
delivery_method: page
delivery_evidence: Community Edition is the free, open-source starting point—perfect for fast pilots and
  proof-of-value.
open_source_license: Apache 2.0
free_self_hosted_tiers:
- Community Edition
managed_premium:
  grade: A
  usd_per_month: 49.0
  plan: Public Cloud Prototype
  price_text: $49 /month
  same_product: verified-by-page
  meaning: the cheapest priced tier beside the free edition on the same page. When same_product is verified-by-name
    it is the provider's own price for operating what it gives away; when unverified it may be a different
    product. Self-hosting cost itself is unmeasured.
estimate_count: 2
estimates:
- kind: entry_cost
  grade: A
  grade_meaning: quoted — the page states this figure
  usd_per_month: 1.0
  meaning: the cheapest published paid tier — a floor on the cost of getting in, NOT the price of the
    API alone
  inputs:
  - file: plans/thingsboard-plans-pricing.yml
    plan: Private Cloud Launch
    price_text: $1 499 /month
  replace_by: 'API Commons Plans — plans[].entries[] with type: fee, a numeric `price`, `timeFrame: month`
    and the tier that includes API access stated in `elements`'
- kind: marginal_cost
  grade: B
  grade_meaning: arithmetic on quoted figures — no assumption
  usd: 0.001
  per: message
  formula: 'monthly price / included quota — an UPPER bound: assumes the quota is fully used'
  inputs:
  - file: plans/thingsboard-plans-pricing.yml
    plan: 'Top-up: Email pack'
    price_text: $1 /month
    included_text: +1000 Emails
  replace_by: 'API Commons Plans — plans[].entries[] with type: metered, `metric` (request | token | transaction
    | message), `unit` and a numeric `price`; and FinOps `primaryUnit` + `meters[]`'
